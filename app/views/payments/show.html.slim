== content_for(:page_name, 'Payments')
h4.mb
  i.fa.fa-angle-righ
  | Your payment
.row.mt
  .col-md-12
    section.task-panel.tasks-widget
      .panel-body
        .task-content
          table.table
            thead
              tr
                p#notice = notice
                th Invoice
                th Amount
                th Currency
                th Client name
                th Payment comments
                th Banking overhead
            tbody
              tr
                td= @payment.invoice_id
                td= @payment.amount
                td= @payment.currency
                td= @payment.client.name
                td= @payment.comments
                td= @payment.banking_overhead
        .inline-block.btn-group
          = link_to payments_path, class: 'btn btn-default btn-xs'
            | Back